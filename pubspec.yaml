name: cricket
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  speech_to_text: ^7.0.0
  printing: ^5.9.1
  pdf: ^3.10.0
  flutter_svg: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - asset/icon/
    

# 👇 Place this OUTSIDE the `flutter:` section
flutter_icons:
  android: true
  ios: true
  image_path: "asset/icon/512sizedCricket.png"
  adaptive_icon_foreground: "asset/icon/512sizedCricket.png"
  adaptive_icon_background: "#FFFFFF"